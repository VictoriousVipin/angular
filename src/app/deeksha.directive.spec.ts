import { DeekshaDirective } from './deeksha.directive';

describe('DeekshaDirective', () => {
  it('should create an instance', () => {
    const directive = new DeekshaDirective();
    expect(directive).toBeTruthy();
  });
});
